import React from "react";

const ROS = ({ id }) => {
  return (
    <div className="box xxx" id={id}>
      <div>ROS</div>
      <span className="status">17% decrease</span>
    </div>
  );
};

export default ROS;
