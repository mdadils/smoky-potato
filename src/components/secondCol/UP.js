import React from "react";

const UP = ({ id }) => {
  return (
    <div className="box xxx" id={id}>
      <div>UP</div>
      <span className="status">17% decrease</span>
    </div>
  );
};

export default UP;
